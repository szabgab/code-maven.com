# SQLite dialect

SQLite works according to the SQL92 standard.

The command line tool has a number of internal commands starting with a dot. For example the `.version`, `.help`, and `.exit` commands.

They can be given interactively or sent through a pipe.
